import React from 'react';
import "./Timeline.css";
import { useParams } from 'react-router-dom'


export default function Timeline() {
    {/*
    const { id } = useParams()
    const project = projectData.find(p => p.id === parseInt(id));
    const status = project.Status;


    return (
        <>
            <ul className="timeline timeline-vertical">
                <li>
                    <hr className="bg-primary" />
                    <div className={`timeline-start timeline-box ${status === "Not Started" ? "bg-gray" : status === "In Progress" ? "bg-gray" : "bg-blue"}`}>Completed</div>
                    <div className={`timeline-middle ${status === "Not Started" ? "gray" : status === "In Progress" ? "gray" : "blue"}`}>

                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" className="text-primary h-5 w-5">
                            <path fillRule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clipRule="evenodd" />
                        </svg>
                    </div>
                    <hr />
                </li>
                <li>
                    <hr />

                    <div className={`timeline-middle ${status === "Not Started" ? "gray" : status === "In Progress" ? "lightblue" : "blue"}`}>
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" className="h-5 w-5">
                            <path fillRule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clipRule="evenodd" />
                        </svg>
                    </div>
                    <div className={`timeline-end timeline-box ${status === "Not Started" ? "bg-gray" : status === "In Progress" ? "bg-lightblue" : "bg-blue"}`}>Under Construction</div>
                    <hr />
                </li>
                <li>
                    <hr />
                    <div className={`timeline-start timeline-box ${status === "Not Started" ? "bg-lightblue" : status === "In Progress" ? "bg-blue" : "bg-blue"}`}>Planning</div>
                    <div className={`timeline-middle ${status === "Not Started" ? "lightblue" : status === "In Progress" ? "blue" : "blue"}`}>
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" className="h-5 w-5">
                            <path fillRule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clipRule="evenodd" />
                        </svg>
                    </div>
                </li>
            </ul>
        </>
    ) */}
}
